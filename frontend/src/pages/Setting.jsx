const Setting = () => {
  return (
    <div className="glass-card max-w-xl p-5">
      <h1 className="mb-2 text-xl font-semibold">Settings</h1>
      <p className="text-sm text-[var(--color-text-muted)]">Personalization and account settings can live here.</p>
    </div>
  );
};

export default Setting;